---
description: 禁止兼容性/过渡代码（除非用户明示）
alwaysApply: true
---

# 禁止兼容性代码

**除非用户在本任务或 issue 中明示允许，不得编写、保留或使用兼容性/过渡代码。**

## 禁止项

- 旧 API / 旧字段 / 旧路由的**读盘兜底**、**双写**、**别名导出**（`@deprecated` 包装、旧名 re-export）
- 启动时或请求时**静默迁移**（cp 旧路径、legacy → 新键 normalize 但不写回）
- 「旧客户端还可能传 X」的**请求体兼容**分支
- 为旧数据格式保留的 **fallback 双路径**（运行时容错除外，见下）

## 允许项

- **用户明示**的短期兼容（须在 PR/任务描述中写清删除条件与截止）
- **插件沙箱**加载失败 → 进程内回退（`plugin-system/loader.ts`）
- **新建用户**一次性 seed（见 `user-data-seed.mdc`）
- 对外部**标准协议**的注释说明（非本项目旧版本）

## 数据已迁移时的做法

1. 删除读旧键/旧路径分支；新格式为唯一真相源。
2. 需要重建的派生数据（Lance FTS、memory 索引）：文档/UI 引导用户**重建**，不在代码里永久读旧布局。
3. 删兼容后更新单测：不再断言 legacy 行为，改为断言新格式或「缺失即失败/空」。

## 新增能力时

- 只实现定案后的 API/字段名；**不做** `toast`/`notify` 式双 API。
- 改存储 schema 时优先一次性迁移脚本或「缺文件即空」；避免每请求 normalize 旧形。
