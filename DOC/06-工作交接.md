# 工作交接（新 Agent）

> 细节以**代码**与 **`DOC/03`** 为准。索引见 [`DOC/README.md`](README.md)。

## 1. 仓库

| 路径 | 用途 |
|------|------|
| `web/` | Vue 3 + Vuetify |
| `server/` | Fastify、chunk、memory、assemble |
| `plugins/` |  bundled 插件源码；运行态 `data/plugins/` |
| `DOC/03` | 实现主文档（§6 轮次、§11 UI、§12 角色、§14 memory、§15 宏） |

## 2. 易错点

- **布局**：`App.vue` 的 `<router-view />` 勿包 `d-flex`；对话壳用 `chat_pane` Grid（`HomeChat` / `useChatSession`）。
- **数据**：chunk **`meta.links` 为权威**；`index` head/tail 可 repair（`DOC/08`）。
- **Memory**：chunk JSON 权威；Lance 在 `{userId}/memory/conversations/{id}/`。**同一 `DATA_DIR` 仅一个 server 进程**（prod+dev 双开会损坏 Lance）。
- **Hybrid FTS**：全局 `user-preferences.json` → `hybridFts`；`zh-jieba` 词典在 `{userId}/hybrid-fts/zh-jieba/{variant}/`；Lance 经 **`LANCE_LANGUAGE_MODEL_HOME`** 定位（`withLanceLanguageModelHome`，勿假设可直传 dict 路径）。详见 `DOC/03` §14.4.3。
- **API Key**：浏览器 GET 无明文；chat/models 服务端 resolve（`DOC/25` §15）。
- **Historian**：插件 id `plot-summary`；自动块指针 `nextBlockStart` / `lastSummarizedEnd`（`DOC/12`）。

## 3. 关键文件

- 对话 UI：`web/src/views/ChatConversationView.vue`、`composables/useChatSession.ts`
- 存储/API：`server/src/chat-storage.ts`、`server/src/index.ts`
- Memory：`server/src/memory-pipeline.ts`、`memory-store.ts`、`memory-index.ts`、`memory-corpus.ts`（语料剥离与召回 query）
- Hybrid FTS：`server/src/hybrid-fts-dict.ts`、`lance-hybrid-search.ts`、`lance-language-model-context.ts`
- 迹录（已实现 v1）：**`DOC/30-plugin-trace-keeper.md`** — `plugins/trace-keeper/`、`host.ui.panel`（`PluginRailHost`，`leftRail`）、`POST …/regenerate-separate`、组装审计 `assembly.plugins`
- Historian：`plugins/plot-summary/src/`
- 组装：`server/src/chat-assemble.ts`

## 4. 阅读顺序

1. `DOC/25` §15（密钥）→ `DOC/03` §4、§14  
2. 改 API → `server/src/index.ts` + 对应 `*-storage.ts`  
3. 改插件 → `DOC/18` + `plugins/*/manifest.json`

## 5. 规则

- 用户未要求：**不** git commit/push。  
- 启动：生产 `npm start`；开发 `npm run dev`（勿与生产共 `DATA_DIR`）。

## 6. 近期变更

### 2026-06-17

- **对话列表**：`vue-virtual-scroller` → **`virtua`**（`ChatMessageList.vue`）；prepend `shift`、滚底重试见 `DOC/15`。
- **思维链**：原生 `<details>` / `<summary>`；sticky + `scroll-state`；移除 session 展开态与展开后滚轮次顶。
- **编辑器保存**：提示词 / 世界书 / Embedding / 对话 API·插件文本字段改为**失焦提交** + 快照去重；世界书整包 `PUT` 2s 限流对齐（`DOC/03` §13.3、§15.10）。
- **对话顶栏**：`chat-header` pill 布局（effective API、绑定提示词、世界书 hover 菜单、设置入口）；见 `DOC/03` §11.2。

### 2026-06-12

- **迹录** `trace-keeper` v1：Together 落盘、Separate 补生成、左 rail、`assembly.plugins` 审计；插件互依赖原则见 **`DOC/09`** §1。
- Historian：摘要前 outgoing 正则 `regexRuleIds` + `regexApplyAllTurns`；手动摘要预填 `[T-buffer-blockTurns, T-buffer]`；re-enable 续接校正 `nextBlockStart`。
- Lance 损坏 → `memory_vector_index_corrupt`；预览弹窗引导重建索引。
- 文档压缩：删 `07/13/16/22`，索引见 `DOC/README.md`。
